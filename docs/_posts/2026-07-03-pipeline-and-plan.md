---
layout: post
title: "Pipeline & Plan"
date: 2026-07-03 12:00:00 -0400
image: pipeline.svg
description: "From raw public data to reproducible scores: how the automated pipeline keeps every number honest."
---

Every number on this site is produced by an automated, tested pipeline:
**raw source files → merged county master table → computed indices → the
dashboard** — with continuous integration that recomputes everything on every
change and fails if any published number no longer matches the data.

*Draft skeleton — Ngan Vu is writing this section (Discord 7/7): brief project
description + a workflow flowchart image (drop it in `docs/images/`).*
